@model IEnumerable<Hotel.Models.Acompaniante>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            cliente
        </th>
        <th>
            Edad
        </th>
        <th>
            Nombres
        </th>
        <th>
            Apellidos
        </th>
        <th>
            Identificacion
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cliente.Identificacion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Edad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nombres)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Apellidos)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Identificacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AcompanianteID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AcompanianteID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AcompanianteID })
        </td>
    </tr>
}

</table>
